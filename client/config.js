// client/config.js — build-time knobs.
// A null SEED means "from the URL ?seed= (integer, or any word hashed) or 1234".
// A null DIFFICULTY means "from the URL ?difficulty= (dusk | blight | hollow) or dusk".
export default { VERSION: '0.1.0-p1', OFFLINE: true, SERVER_URL: '', SEED: null, DIFFICULTY: null };
